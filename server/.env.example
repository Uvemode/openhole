# Name of the Zellij session running Claude Code
ZELLIJ_SESSION=claude

# MCP server bind address - keep 127.0.0.1 to stay local only
HOST=127.0.0.1

# Port the MCP server listens on (SSH tunnel should forward this)
PORT=3000
